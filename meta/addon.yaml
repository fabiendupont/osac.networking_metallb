name: osac-networking-metallb
display_name: Networking (MetalLB)
description: L2/BGP load balancer IP advertisement via MetalLB for OSAC
version: 0.1.0

dependencies:
  osac_core: ">=0.1.0"

resource_types:
  - name: ExternalIPPool
    scope: platform
  - name: ExternalIP
    scope: tenant
  - name: ExternalIPAttachment
    scope: tenant
