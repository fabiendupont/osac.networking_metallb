resource_type: ExternalIPPool
event: Create
phase: main
priority: 100
failure_policy: Fail

parameters:
  - name: external_ip_pool
    type: dict
    required: true
    description: ExternalIPPool Kubernetes resource

outputs:
  - name: pool_name
    type: string
    description: Created IPAddressPool name
