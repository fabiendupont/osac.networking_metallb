resource_type: PublicIPPool
event: Delete
phase: main
priority: 100
failure_policy: Fail

parameters:
  - name: public_ip_pool
    type: dict
    required: true
    description: PublicIPPool Kubernetes resource

outputs: []
