resource_type: ExternalIP
event: Create
phase: main
priority: 100
failure_policy: Fail

parameters:
  - name: external_ip
    type: dict
    required: true
    description: ExternalIP Kubernetes resource

outputs:
  - name: assigned_ip
    type: string
    description: IP address assigned by MetalLB
